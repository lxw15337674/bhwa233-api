{
    "version": 2,
    "buildCommand": "pnpm build",
    "outputDirectory": "dist",
    "devCommand": "pnpm start:dev",
    "installCommand": "pnpm install",
    "regions": ["iad1"],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/main.js"
        }
    ]
}